// TODO: Implement Invoice model with Mongoose schema
// Invoice schema fields (for reference during Mongoose implementation):
// _id, userId, number, clientId, client.name, client.email, amount,
// status (draft, sent, paid, overdue, void), date, dueDate,
// description, items (array with description, quantity, unitPrice, total),
// createdAt, updatedAt